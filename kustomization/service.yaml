apiVersion: v1
kind: Service
metadata:
  labels:
    app: workspace
  name: workspace
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: workspace
  type: ClusterIP