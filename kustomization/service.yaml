apiVersion: v1
kind: Service
metadata:
  labels:
    app: workspace
  name: workspace
  namespace: default
spec:
  ports:
  - name: ssh
    port: 22
    protocol: TCP
    targetPort: ssh
  selector:
    app: workspace
  type: ClusterIP
  externalIPs:
  - 2.59.135.54
  - 2.59.134.144
  - 193.42.11.58